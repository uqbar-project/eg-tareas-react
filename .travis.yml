language: node_js

node_js:
  - 12.18

addons:
  apt:
    packages:
    - libgconf-2-4

cache:
  directories:
    - ~/.npm
    - ~/.cache
  override:
    - npm ci
    - npm run cy:verify

script:
  - npm run start:ci
  - npm run test
  - npm run cy:run